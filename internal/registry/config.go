// Package registry maintains the asdf plugin registry.
package registry

// defaultURL is the upstream asdf plugin index.
const defaultURL = "https://github.com/asdf-vm/asdf-plugins.git"

// upstreamPluginsSubdir is the directory inside the `asdf-plugins` repo that holds per-tool entries.
const upstreamPluginsSubdir = "plugins"
